# 翼览无余 · 后端依赖清单（Python >= 3.11）
# 与 pyproject.toml [project].dependencies 保持一致。
# 提示：项目同时附带 uv.lock，如已安装 uv 可直接 `uv sync` 精确还原环境（含传递依赖锁定）。
cryptography==49.0.0
grpcio==1.82.1
protobuf==7.35.1
pypdf>=6.14.2
websockets==15.0.1
langgraph==1.2.9
langgraph-checkpoint-sqlite==3.1.0
httpx[http2]>=0.28.1
sentence-transformers>=5.6.0
structlog>=26.1.0
jieba>=0.42.1
faster-whisper>=1.0.3,<2
edge-tts>=7.2,<8
