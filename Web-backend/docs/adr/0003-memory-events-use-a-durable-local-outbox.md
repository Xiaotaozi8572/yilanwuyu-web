# 记忆事件通过双投递箱可靠交付

Java 长期记忆服务不可用时，Python 回答主链继续运行，并将最小化的待治理记忆事件暂存到有期限、可重试的本地可靠投递箱；恢复后通过 `SubmitMemoryEvents` gRPC 使用事件 ID 幂等重放。Java 在同一 PostgreSQL 事务中写入交互事件和 Java 事务投递箱后才确认接收，再由内部发布器送入 Redis Streams。两级投递箱只承担可靠传输，不是第二套长期记忆库，不允许回答链读取，也不保存原始音频或完整聊天记录。
