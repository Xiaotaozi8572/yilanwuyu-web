# 单机记忆服务仍采用零信任边界

Python 与 Java 的 gRPC 使用双向 TLS 和服务身份校验，Python 不直接持有 PostgreSQL、Neo4j 或 Redis 凭据；学习者身份来自签名会话，管理 REST 按学习者、管理员和审计员授权。所有数据服务仅绑定私有网络，高隐私载荷应用级加密，秘密从运行环境注入，日志不得输出记忆正文、原始对话、完整 Prompt 或真实身份；竞赛演示只使用临时匿名账户与临时证书。
