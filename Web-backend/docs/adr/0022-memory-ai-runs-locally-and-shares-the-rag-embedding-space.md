# 记忆智能离线运行并与 RAG 共享向量空间

后台 Python 记忆 worker 使用本地量化 `Qwen3-4B-Instruct-2507` 与 llama.cpp 生成复杂候选，RAG 和记忆统一使用版本锁定的 `BAAI/bge-m3`。Python 对当前问题只计算一次查询向量，并向 Java 传递模型 ID、版本、维度与归一化信息；Java 遇到向量元数据不匹配时关闭语义通道而继续结构化、关键词和时间图检索。重排模型仅用于离线评测或后台校准，所有模型输出仍需契约、来源与 Java 治理，模型不可用不得影响回答主链。
