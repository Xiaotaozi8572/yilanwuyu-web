# 记忆缓存使用版本化键并对隐私变更强一致失效

每位学习者维护单调递增的 `memory_epoch` 与独立 `policy_epoch`，Caffeine 和 Redis 缓存键包含身份、查询指纹、场景摘要、契约版本及相应 epoch。普通更新通过新 epoch 使旧缓存不可达；撤回授权、遗忘和高隐私变化先在 PostgreSQL 事务中更新策略版本，再同步清除本机缓存，后续读取必须验证最新策略。Redis 通知只加速清理，不能承担隐私正确性；缓存只保存最小化记忆上下文投影。
