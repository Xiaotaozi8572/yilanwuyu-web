# 记忆读取区分已应用、合法为空和技术降级

每次记忆读取必须产生 `APPLIED`、`EMPTY` 或 `DEGRADED` 状态：成功使用记忆为 `APPLIED`，匿名、未授权或无相关记忆为合法 `EMPTY`，超时、不可用、响应损坏或契约不兼容为 `DEGRADED` 且丢弃全部返回内容。身份不匹配或越权按高严重度事件审计并熔断相关读取，但 Python 仍以空记忆上下文完成有证据回答；状态只进入脱敏追踪和监控，不修改公开回答契约。
