# 记忆上下文采用无在线 LLM 的混合检索

记忆读取先执行身份、授权、隐私、有效时间和状态硬过滤，再并行运行结构化精确、关键词、pgvector 语义、Neo4j 时间关系和情景近期性检索，使用 RRF 融合与规则重排。策略记忆强制进入上下文，其余记忆按类型和 token 预算选择并标注 `required/optional/prohibited`；实时 120 毫秒路径禁止调用 LLM，抽取、反思和候选生成只在后台执行。
