# Python 提出智能记忆候选，Java 治理并持久化

Java 负责长期记忆工作流、权限、状态机、冲突处理、时间治理、事务和最终写入，并可直接处理明确偏好等确定性事件。会话摘要、复杂误解、反思和关系发现由独立 Python 记忆智能 worker 异步生成带来源与置信度的候选；Python worker 无权直接写 PostgreSQL、Neo4j 或缓存，所有候选必须经 Java 契约与隐私验证后接受、拒绝或等待确认。
