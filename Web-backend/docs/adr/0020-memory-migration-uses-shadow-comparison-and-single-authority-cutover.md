# Python 记忆迁移采用影子比较和单一权威切换

迁移先冻结现有记忆上下文、事件与治理契约，并将 SQLite 数据校验后导入 PostgreSQL；Java 以影子模式消费事件并生成不影响回答的对照上下文，直到允许项、禁止项、偏好、误解和时间状态差异达到验收门槛。切换通过配置开关完成，期间禁止双权威写入；切换后 Java 不可用即使用空记忆上下文，Python 旧实现只保留为受控迁移适配器，并在完整回归后再决定清理。
