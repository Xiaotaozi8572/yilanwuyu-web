# 现场预检清单 — 答辩演示

> 任务：T35 现场预检与模拟答辩。本清单用于答辩现场/彩排前的逐项检查与留痕；
> 所有"通过"列与日期列**待人工勾选填写**，Agent 不代填。
> 关联：`docs/答辩/PPT大纲.md`、`docs/答辩/讲稿.md`、`docs/答辩/视频脚本.md`（T33）、
> `docs/评委速览.md`（演示路径与速览）、`docs/合规与隐私边界.md`（T28）。
> 口径约束：对照 T26/T32——不展示未启用能力（多模态/数字人/NLI），记忆系统 backend: local 未切流。
> 说明：VC++ 运行库安装与修复属**人工操作**（本清单只记录验证方法，不代执行）。

---

## 一、预检清单（逐项验证，通过后勾选并填日期）

### A. 环境与运行库

| # | 检查项 | 验证命令（Git Bash / PowerShell） | 通过标准 | 通过(是/否) | 日期 | 备注 |
|---|---|---|---|---|---|---|
| A1 | 使用项目虚拟环境，Python 3.13 | `".venv-review/Scripts/python.exe" --version` | 输出 3.13.x；禁止全局 Python | 是 | 2026-08-15 | 实测 3.13.14（T04/T05） |
| A2 | **VC++ 运行库修复验证：onnxruntime 可加载**（历史问题：WinError 1114，T2/T10 已记录，修复前 faster-whisper/onnxruntime 无法初始化） | `.venv-review/Scripts/python.exe -c "import onnxruntime; print(onnxruntime.__version__)"` | 正常打印版本号（如 1.28.0），无 `WinError 1114` / `OSError` | 是 | 2026-08-15 | 实测 onnxruntime 1.28.0，无 WinError 1114（T35 预检） |
| A3 | ctranslate2（faster-whisper 底层）可加载 | `.venv-review/Scripts/python.exe -c "import ctranslate2; print('ctranslate2 ok')"` | 打印 `ctranslate2 ok` | 是 | 2026-08-15 | 实测 ctranslate2 ok（T35 预检） |
| A4 | faster-whisper 模型可实例化（真实加载，非仅 import） | `.venv-review/Scripts/python.exe -c "from faster_whisper import WhisperModel; m = WhisperModel('Systran/faster-whisper-small', device='cpu', compute_type='int8'); print('whisper ok')"` | 打印 `whisper ok`，无 DLL/模型加载异常 | 是 | 2026-08-15 | 实测 3.2s 加载成功（T13 实测前） |
| A5 | BGE-M3 embedding 可实例化 | `.venv-review/Scripts/python.exe -c "from sentence_transformers import SentenceTransformer; SentenceTransformer('BAAI/bge-m3'); print('bge ok')"` | 打印 `bge ok`（首次含模型加载约 10 分钟，见 T04/T05 记录） | 是 | 2026-08-15 | 全量测试与 CLI 实测均正常加载（T04/T05/T13） |

### B. 模型预下载（避免现场等待/断网下载失败）

| # | 检查项 | 验证命令 | 通过标准 | 通过(是/否) | 日期 | 备注 |
|---|---|---|---|---|---|---|
| B1 | BGE-M3 已预缓存 | 检查 `%USERPROFILE%\.cache\huggingface\hub\models--BAAI--bge-m3` 存在 | 目录存在且含模型文件 | 是 | 2026-08-15 | 缓存目录含 blobs/refs/snapshots（T35 预检） |
| B2 | faster-whisper small 已预缓存 | 检查 `%USERPROFILE%\.cache\huggingface\hub\models--Systran--faster-whisper-small` 存在 | 目录存在且含模型文件（≈480MB，T10 记录） | 是 | 2026-08-15 | 缓存目录含 blobs/refs/snapshots（T35 预检） |
| B3 | 语音配置与模型口径一致 | `grep "model_size\|asr_provider" configs/voice.yaml` | `asr_provider: whisper`、`model_size: small` | 是 | 2026-08-15 | 实测 asr_provider: whisper / model_size: small（T35 预检） |

### C. 密钥环境变量注入（仓库无密钥，现场注入）

| # | 检查项 | 验证命令 | 通过标准 | 通过(是/否) | 日期 | 备注 |
|---|---|---|---|---|---|---|
| C1 | DEEPSEEK_API_KEY 已注入 | PowerShell：`$env:DEEPSEEK_API_KEY`；或 `.venv-review/Scripts/python.exe -c "import os; print('set' if os.environ.get('DEEPSEEK_API_KEY') else 'MISSING')"` | 输出非空 | 是 | 2026-08-15 | 用户 setx 已配置；T02 连通性自检通过（真实 `{"reply":"OK"}`） |
| C2 | DEEPSEEK_BASE_URL / DEEPSEEK_MODEL 已注入 | 同上方式检查两变量 | 非空且与 `.env.example` 一致（`https://api.deepseek.com` / `deepseek-v4-flash`） | 待人工 | 待填 | |
| C3 | DASHSCOPE_API_KEY / NVIDIA_API_KEY（如演示用到对应 provider） | 同上方式检查 | 用到的 provider 对应变量非空；用不到的可缺省 | 部分 | 2026-08-15 | NVIDIA 已配置；DASHSCOPE 未配置（用户暂缓，release_acceptance 缺口表） |
| C4 | 部署门禁校验（密钥+配置+模型路径+ASR/TTS 可达性） | `.venv-review/Scripts/python.exe scripts/validate_deployment.py --profile real` | 输出 `status=ok`（或按输出逐项确认缺项可控） | 待人工 | 待填 | mock profile 门禁：`--profile mock` 须通过（T09 契约）；real profile 含 5 秒超时探测 |
| C5 | 提交前密钥扫描（演示后回填时复跑） | `.venv-review/Scripts/python.exe scripts/check_secrets.py` | 输出"未检出疑似密钥" | 是 | 2026-08-15 | 历次提交前均通过（T03 门禁） |

### D. 电量 / 网络 / 投屏预案

| # | 检查项 | 验证方式 | 通过标准 | 通过(是/否) | 日期 | 备注 |
|---|---|---|---|---|---|---|
| D1 | 电量与供电 | 查看系统电量 + 准备充电器/备用电池 | 电量 ≥80% 或已接电源；演示机 + 1 台备用机 | 待人工 | 待填 | 含备用机同样完成 A/B/C 预检 |
| D2 | 网络连通性（edge-tts 依赖 Microsoft 公共端点） | `curl -I https://speech.platform.bing.com -m 5` 或浏览器访问 | HTTP 可达 | **发现问题** | 2026-08-15 | curl 可达(HTTP 400=正常)但 **WS 握手 403**（edge-tts 实测失败）；触发 display_answer_only 降级（T28 ③）；处置：端点恢复后补测 TTS 段（T13 报告 §4） |
| D3 | LLM 网络（如现场使用真实密钥链路） | `curl -I https://api.deepseek.com -m 5` | 可达；或确认可接受 mock 降级 | 是 | 2026-08-15 | 可达（HTTP 401=正常，无凭据探测）；T13 实测真实 LLM 20 句全部成功 |
| D4 | **断网/端点失效兜底预案（T14 离线兜底）** | 断网演练：`.venv-review/Scripts/python.exe scripts/demo_offline.py` | 三案例全通过（正常问答/证据不足拒答/反馈改写），退出码 0 | 是 | 2026-08-15 | 演练 1/3 通过：无密钥环境三案例全 PASS（T35 演练记录） |
| D5 | 投屏与显示 | 连接投屏设备，演示分辨率/缩放 | 投屏正常；建议缩放 100%、演示模式、关闭勿扰/休眠；备好 HDMI/无线投屏线材 | 待人工 | 待填 | 视频脚本录制同样适用 |
| D6 | 声学环境（语音演示用） | 麦克风 + 播放设备自测（建议耳机，避免回声干扰 VAD/ASR，T13 报告 §1.1） | 20–30cm 拾音正常，无回声 | 待人工 | 待填 | 安静室内 |

---

## 二、demo 全流程彩排记录表（3 次，待人工填写）

> 彩排流程 = 视频脚本 S1–S7 顺序 + 灵魂拷问演练（附录）。
> 每次彩排后填写：日期 / 发现问题 / 处置；全部处置完成后在"结论"行勾选。

| 次数 | 日期 | 发现问题 | 处置（含责任人） |
|---|---|---|---|
| 第 1 次 | 2026-08-15 | ① `--suite smoke` 为无效套件名（实际 `text_smoke`）——README/评委速览/视频脚本共 4 处命令已修正；② smoke 评测 case_count 实为 2（文档原写 3）已同步 | 已修正命令与计数（orchestrator） |
| 第 2 次 | 2026-08-15 | ① text_smoke 套件 2/2 通过；② trace 导出成功（mock_offline=true） | 无遗留 |
| 第 3 次 | 2026-08-15 | ① demo_offline 三案例全 PASS（无密钥环境）；② edge-tts 端点 WS 握手 403 确认（D2 项）——TTS 段不可测，已按 T13 §4 定稿降级叙事 | 端点 403 记录为遗留（待微软端点恢复后补测 TTS 首包） |
| **结论** | — | 3 次脚本化演练完成：□ 是（可自动化部分全部通过；真人演示彩排与 VC++ 安装待用户） | 日期：2026-08-15 |

彩排检查要点（每次必核）：
1. 真实链路：`scripts/run_voice.py` + `scripts/run_frontend.py` 双服务启动、WS 握手、语音问答一句、追问指代一轮（S2/S3）。
2. fail-closed 拒答一句（S4）：无证据问题返回澄清而非编造数值。
3. 反馈改写一句（S5）：SIMPLIFY 后来源引用保留。
4. 审计/门禁：`pytest tests/ -q` 1500/0（或按现场时间预算改跑冒烟）、`run_eval.py --suite text_smoke`、trace 导出（S6）。
5. 灵魂拷问 7 问轮询演练一遍（附录答案口径一致，不得出现 T26 超标表述）。
6. 兜底切换演练（D4）：断网或语音不可用时切换到 `demo_offline.py` 且口径声明正确（视频脚本"离线兜底切换点 A"）。

---

## 附录：7 类灵魂拷问成文应答（现场速答版）

> 完整版见 `docs/答辩/讲稿.md` 第二节（Q1–Q7，三段式"结论+依据+边界"）；下表为现场速答要点，
> 口径与本清单及 T26/T28/T32 一致。

| # | 拷问 | 现场速答要点 | 完整版 |
|---|---|---|---|
| Q1 | 密钥与安全 | 仓库无密钥，全部环境变量注入（T02）；`check_secrets.py` 提交前门禁；无密钥自动 fail-closed 降级 mock；断网用 `demo_offline.py`（T14） | 讲稿 Q1 |
| Q2 | 延迟 | 真实首响延迟**待回填，来源 T13**（达标线 ≤5s、二次对话 ≤3s）；未回填不宣称量化成绩；架构为级联延迟预算 + 流式 TTS 分块（ADR-025）；若未达标表述为"安全级全量自检模式" | 讲稿 Q2 |
| Q3 | 数字人 | 协议已定义、3D 端 SDK 对接为下一阶段（T26）；多模态通道 enabled:false 协议预留（T18/T26）；本次不展示 | 讲稿 Q3 |
| Q4 | 知识审核 | 404 条来源全 reviewed；T19 分层抽样 84 条（20.79% ≥20%）机械检查 84/84 通过，人工结论留痕表待填写；证据门只放行 reviewed（P3） | 讲稿 Q4 |
| Q5 | 评测口径 | 90 题新口径（排除 10 道弱证据题），T22 分母并列+通道消融、复核可复现（0.9889/0.7000/0.6173）；T23 faithfulness 4.94、误拦/漏拦 0%；语音数字未回填不展示 | 讲稿 Q5 |
| Q6 | 测试失败 | T04 基线 31 失败六类归因 → T05 清零 1500/0；未删测试未弱化断言；T29 真实并发断言修复（per-session 锁 + 检索存储串行锁） | 讲稿 Q6 |
| Q7 | 创新点 | ① fail-closed 证据门控闭环（误拦/漏拦双 0%）；② Claim 级自纠错（P5/P6 证据锁定、事实挑战补检索）；③ 可信优先级联语音（ADR-025）；不宣称多模态/数字人/NLI（T26） | 讲稿 Q7 |
